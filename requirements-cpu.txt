# CPU-only requirements - Install in this exact order!
# Step 1: PyTorch CPU version (must be first!)
torch
torchvision
torchaudio

# Step 2: PaddlePaddle CPU version (must be second!)
--find-links https://www.paddlepaddle.org.cn/packages/stable/cpu/
paddlepaddle==3.0.0

# Step 3: OCR libraries (must be third!)
paddleocr==3.0.0

# Step 4: Finally, base requirements
-r requirements-base.txt
