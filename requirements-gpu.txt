# GPU-specific requirements (CUDA 12.6) - Install in this exact order!
# Step 1: PyTorch with CUDA support (must be first!)
--index-url https://download.pytorch.org/whl/cu126
torch
torchvision
torchaudio

# Step 2: PaddlePaddle GPU version (must be second!)
--find-links https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.0.0

# Step 3: OCR libraries (must be third!)
paddleocr==3.0.0

# Step 4: Finally, base requirements
-r requirements-base.txt
